# 本地開發環境配置（複製為 .env.local 並修改）
# 此文件不會被 Git 追蹤，用於本地 Apache 環境

# 本地 Apache 環境 API 端點
VITE_API_BASE_URL=https://accounts.huabing.test/api
VITE_APP_ENV=local
VITE_APP_NAME=帳號管理中心（本地）

# Google OAuth (本地開發)
VITE_GOOGLE_CLIENT_ID=your-local-google-client-id

# 本地調試設定
VITE_DEBUG_MODE=true
VITE_API_TIMEOUT=5000

# 特殊功能開關
# VITE_ENABLE_MOCK_API=false